import 'package:flutter/material.dart';

class ArriveFarmScreen extends StatelessWidget {
  const ArriveFarmScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('Registro Llegada a Granja'));
  }
}
