import 'package:flutter/material.dart';

class CloseBinScreen extends StatelessWidget {
  const CloseBinScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Registrar Cerrar Bines'),
    );
  }
}
